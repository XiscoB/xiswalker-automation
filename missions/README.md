# Missions Directory

This directory stores user-created validation missions and visual templates.

**Contents:**
- `atomic/` — Reusable single-purpose sequences
- `composite/` — YAML orchestrations of atomic missions
- `templates/` — PNG reference images for visual matching

**Note:** This directory is excluded from version control (`.gitignore`).
Only example/demo content should be committed; personal missions should remain local.
